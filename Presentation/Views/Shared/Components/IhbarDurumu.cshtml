@model List<SelectListItem>
@if (Model.Count > 0)
{
    <select asp-items="Model" class="form-control" id="ihbarDurumu" name="ihbarDurumu">
        <option value="">-- Seçiniz --</option>
    </select>
}
